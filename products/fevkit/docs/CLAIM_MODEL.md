# Claim model

Claims are first-class records rather than prose hidden in a final answer. Every claim declares its kind, risk, producing steps, typed evidence edges, uncertainty, limitations, and—when inferential—a rationale.

This makes unsupported and over-broad statements auditable without pretending that a deterministic rule can judge biological truth. Contradictory evidence is retained through `contradicted_by` edges rather than silently discarded.